#include "main.h"
/**
* print_square - Print - square
*
* @size: int n parameter
* Description: 'print square with #'
*/
void print_square(int size)
{
	int c;
	int r;

	for (c = 0; c < size; c++)
	{
		for (r = 0; r < size; r++)
		{
			_putchar(35);
		}
		_putchar('\n');
	}
	if (n <= 0)
		_putchar('\n');
}
