#include "main.h"
/**
* _strlen - reset to 98
*
* @s: s is a ponter to char data type
* Description: 'count the length'
* Return: int value 0 (Succuccess)
*/
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
	}

	return (i);
}
