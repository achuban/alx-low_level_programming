#include "main.h"
/**
* _puts - reset to 98
*
* @s: s is a ponter to char data type
* Description: 'count the length'
*/
void _puts(char *str)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
		_putchar(s[i]);
	}
	_putchar('\n');

	return (i);
}
